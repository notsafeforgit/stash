fragment PreviewImageData on PreviewImage {
  ...PreviewImageRenditionData
  thumbnail {
    ...PreviewImageRenditionData
  }
}

fragment PreviewImageRenditionData on PreviewImage {
  fallback
  sources {
    url
    mime_type
    dynamic_range
    width
    height
  }
}
